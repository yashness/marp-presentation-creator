[app]
name = "Marp Builder API"
version = "1.0.0"
debug = false

[server]
host = "0.0.0.0"
port = 8000
workers = 4

[logging]
level = "INFO"
rotation = "500 MB"
retention = "10 days"

[marp]
default_theme = "default"
export_formats = ["pdf", "html", "pptx"]
cli_path = "marp"
